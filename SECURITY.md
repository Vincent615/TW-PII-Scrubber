# 安全性回報 / Security Policy

本工具的核心承諾是「**資料不離機**」。以下情況視為安全漏洞,請回報:

- 任何會使資料離開本機的行為(對外連線、遙測、非預期的檔案落地)
- 遮罩座標錯位(遮錯位置、真實個資殘留在輸出中)
- 稽核報告、ZIP 或任何輸出含未遮罩的原值
- 可繞過偵測的系統性手法(非單一模型漏測)

## 回報方式

- 請使用 GitHub 的 **Private Vulnerability Reporting**
  (repo 頁面 → Security → Report a vulnerability)私下回報。
- **請勿**在公開 issue 中貼出含真實個資的重現案例;
  請一律改用合成資料(如 `A123456789`、`王小明`)描述重現步驟。

單純的模型漏測(某個名字沒被 CKIP 辨識出來)請走一般 issue,
並參考 README「已知限制」章節。
